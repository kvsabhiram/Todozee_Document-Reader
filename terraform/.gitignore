# Terraform state & local secrets
*.tfstate
*.tfstate.*
.terraform/
# .terraform.lock.hcl IS committed (pins provider versions for reproducible
# installs across local + CI). Only the .terraform/ cache dir is ignored.
crash.log
crash.*.log

# Generated SSH private key and tfvars
*.pem
terraform.tfvars
*.auto.tfvars
